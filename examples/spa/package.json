{
  "private": true,
  "scripts": {
    "build": "webpack",
    "clean": "rm -rf dist",
    "start": "npm install; if [[ ${NODE_ENV} == \"production\" ]]; then npm run start:prod; else npm run start:dev; fi",
    "start:dev": "npm run clean && webpack-dev-server --content-base public/ --hot --inline",
    "start:prod": "npm run clean && npm run build && (cd public; python -m SimpleHTTPServer 3000)"
  },
  "dependencies": {
    "axios": "0.5.4",
    "babel-core": "5.8.23",
    "babel-loader": "5.3.2",
    "es6-promise": "3.0.2",
    "react": "0.13.3",
    "react-router": "0.13.3",
    "webpack": "1.12.1"
  },
  "devDependencies": {
    "webpack-dev-server": "1.10.1"
  }
}
