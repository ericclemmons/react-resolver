{
  "devDependencies": {
    "axios": "^0.5.2",
    "babel-loader": "^4.2.0",
    "babelify": "^5.0.4",
    "browser-sync": "^2.4.0",
    "es6-promise": "^2.0.1",
    "react": "^0.13.1",
    "react-router": "^0.13.2",
    "watchify": "^2.6.0"
  },
  "main": "main.js",
  "private": true,
  "scripts": {
    "browser-sync": "browser-sync start --files 'public/**' --server public --no-open",
    "start": "npm run watchify & npm run browser-sync",
    "watchify": "watchify -d -t babelify -v -e main.js -o public/main.min.js"
  }
}
