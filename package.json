{
  "name": "react-resolver",
  "version": "2.0.22",
  "description": "**Browserify React duplicate WORKAROUND** -- Async-rendering & data-fetching for universal React applications",
  "main": "dist/exporter.js",
  "devDependencies": {
    "babel": "^5.8.0",
    "react": "^0.14.0",
    "react-dom": "^0.14.0"
  },
  "scripts": {
    "deploy": "babel --stage 0 ./src --out-dir ./dist; git add --all .; git commit -am 'pushing to git'; git push origin HEAD; echo 'done';"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "homepage": "https://github.com/ericclemmons/react-resolver"
}
